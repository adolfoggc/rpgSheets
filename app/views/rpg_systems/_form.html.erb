<%= form_for(rpg_system) do |f| %>
  <% if rpg_system.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(rpg_system.errors.count, "error") %> prohibited this rpg_system from being saved:</h2>

      <ul>
      <% rpg_system.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
